% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spk_rast_rm_empty.R
\name{spk_rast_rm_empty}
\alias{spk_rast_rm_empty}
\title{Remove Empty Raster Files}
\usage{
spk_rast_rm_empty(
  path = NULL,
  delete = TRUE,
  regexp = "\\\\.(tif|tiff|vrt)$",
  quiet = FALSE
)
}
\arguments{
\item{path}{\link{character} A single path to search for raster files.}

\item{delete}{\link{logical} Whether to delete the empty files. Default is \code{TRUE}.}

\item{regexp}{\link{character} A regular expression passed to \code{\link[fs:dir_ls]{fs::dir_ls()}} to select raster files. Default is '\.(tif|tiff|vrt)$'.}

\item{quiet}{\link{logical} If \code{FALSE}, prints messages about files that are or can be removed. Default is \code{FALSE}.}
}
\value{
\link{character} A character vector of paths to the raster files that were empty. Returns invisibly.
}
\description{
Scans a directory for raster files (e.g., .tif, .tiff, .vrt) and removes those that contain only zero values.
Uses \code{\link[=spk_rast_not_empty]{spk_rast_not_empty()}} to check each raster.
}
\examples{
\dontrun{
# Remove empty rasters from a directory
spk_rast_rm_empty("path/to/rasters")
}

}
\seealso{
\code{\link[=spk_rast_not_empty]{spk_rast_not_empty()}}, \code{\link[fs:dir_ls]{fs::dir_ls()}}, \code{\link[fs:delete]{fs::file_delete()}}, \code{\link[terra:rast]{terra::rast()}}, \code{\link[terra:values]{terra::values()}}

Other raster: 
\code{\link{spk_rast_ext}()},
\code{\link{spk_rast_not_empty}()},
\code{\link{spk_res}()}
}
\concept{raster}
